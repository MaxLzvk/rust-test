services:
  pokemon-trader:
    build: ./pokemon-trader

  rust-app:
    build: ./rust-app

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - pokemon-trader
      - rust-app
